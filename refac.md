# Power BI Solution Documentation

Generated by Langchain Documentation Agent (V4.1)
Date: 2025-04-25 16:56:56

## Table of Contents

* [1. Overview](#1-overview)
* [2. Data Model](#2-data-model)
  * [2.1 Tables (incl. Columns & Measures)](#21-tables-columns-measures)
  * [2.2 Relationships](#22-relationships)

---

## <a name="1-overview"></a>1. Overview

The Power BI solution appears to focus on analyzing timesheet data, specifically related to project management and employee performance. The main goal of the report is to provide insights into hours worked, revenue generated, and average hourly rates across various projects and employees. Key metrics include total hours, sales amounts, average hourly rates, project margins, and realization percentages, which are essential for evaluating project profitability and resource allocation.

The dataset is built from multiple tables, including `vw_Timesheet`, `tbl_Project`, and `DimDate`, with significant measures such as `Sum(vw_Timesheet.Hours)`, `Sum(vw_Timesheet.SalesAmount)`, and `vw_Timesheet.MSR_AVG_HOURLY_RATE`. The visuals primarily consist of pivot tables that allow users to drill down into data by dimensions such as project, employee, and time periods (weeks and years). The intended audience for this report likely includes project managers, financial analysts, and other stakeholders involved in project oversight and financial performance evaluation.

---

## <a name="2-data-model"></a>2. Data Model

### <a name="21-tables-columns-measures"></a>2.1 Tables (incl. Columns & Measures)

#### Table: `database`

Purpose not clear from context.

##### Regular Columns
_(No regular columns identified.)_

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `DimDate`

The `DimDate` table is likely used for date-related analysis, providing various attributes of dates such as day names, week numbers, month names, and flags for the current year. The presence of columns like `ShortDate`, `DayName`, and `CalendarYear` suggests it serves as a date dimension in a data model, facilitating time-based reporting and analysis.

##### Regular Columns
- `CalendarQuarter` (string): No additional description provided.
- `CalendarYear` (int64): No additional description provided.
- `DateKey` (int64): No additional description provided.
- `DayName` (string): No additional description provided.
- `DayNumberOfMonth` (int64): No additional description provided.
- `DayNumberOfWeek` (int64): No additional description provided.
- `DayNumberOfYear` (int64): No additional description provided.
- `MonthName` (string): No additional description provided.
- `MonthNumberOfYear` (int64): No additional description provided.
- `ShortDate` (dateTime): No additional description provided.
- `WeekNumberOfYear` (int64): No additional description provided.

##### Calculated Columns
- **`CurrentYearFlag`**:
  ```dax
IF(YEAR(DimDate[ShortDate]) <= YEAR(TODAY()), 1, 0)
  ```

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `Hours_nd_Percentage`

The 'Hours_nd_Percentage' table appears to be designed to manage and summarize data related to hours worked and their corresponding percentages. It includes columns for actual hours, percentage of hours, and an order for sorting, with annotations indicating automatic summarization settings.

##### Regular Columns
- `'Hours_nd_Percentage Fields'` (DataType): No description provided.
- `'Hours_nd_Percentage Order'` (DataType): No description provided.
- `Hours_nd_Percentage` (DataType): No description provided.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `lkp_Code`

The `lkp_Code` table appears to serve as a lookup table containing codes, qualification values, and sorting information. It includes columns for `Code`, `Qualify`, and `Sort`, with data sourced from an Excel workbook. The annotations suggest that summarization settings are automatically determined for each column.

##### Regular Columns
- `Code` (string): No additional description provided.
- `Qualify` (int64): Summarized by sum.
- `Sort` (string): No additional description provided.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `lkp_fltr_Employee`

The table 'lkp_fltr_Employee' appears to serve as a lookup table for employee information, containing columns for employee identifiers and names. It is sourced from an Excel workbook and includes annotations indicating automatic summarization settings.

##### Regular Columns
- `EmployeeId` (string): No additional description provided.
- `EmployeeName` (string): No additional description provided.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `lkp_Project`

The `lkp_Project` table appears to serve as a lookup table containing project-related information, including project names, qualification metrics, billing amounts, and associated groups. The columns are defined with specific data types and summarization settings, indicating their intended use in reporting and analysis.

##### Regular Columns
- `Billing` (int64): Summarized by sum.
- `Group` (string): No additional description provided.
- `Project` (string): No additional description provided.
- `Qualify` (int64): Summarized by sum.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `lkp_Unit`

The `lkp_Unit` table appears to serve as a lookup table for organizational units, containing columns that represent various attributes such as the organizational unit name, billable department, and unit identifiers. The data is sourced from an Excel workbook, and the columns are defined with specific data types and summarization settings.

##### Regular Columns
- `BillableDep` (int64): No additional description provided.
- `OWN-Sub-ExtT` (string): No additional description provided.
- `Organisatorische eenheid` (string): No additional description provided.
- `Unit` (string): No additional description provided.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `model`

Purpose not clear from context.

##### Regular Columns
_(No regular columns identified.)_

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `tbl_Project`

The `tbl_Project` table appears to be designed for managing project-related data, including project identifiers, descriptions, statuses, financial information, and client details. The presence of various columns such as `ProjectKey`, `Project`, `Status`, and `ClientName` suggests that it serves as a comprehensive repository for tracking project attributes and their associated metadata.

##### Regular Columns
- `ActualDateCompleted` (dateTime): No regular columns identified under this header.
- `Administration` (string): No regular columns identified under this header.
- `Changed_on` (dateTime): No regular columns identified under this header.
- `ClientName` (string): No regular columns identified under this header.
- `ClientNumber` (string): No regular columns identified under this header.
- `Created_on` (dateTime): No regular columns identified under this header.
- `Currency` (string): No regular columns identified under this header.
- `DateCreated` (dateTime): No regular columns identified under this header.
- `DevoteamPillar` (string): No regular columns identified under this header.
- `PlannedCompletionDate` (dateTime): No regular columns identified under this header.
- `Project` (string): No regular columns identified under this header.
- `ProjectDescription` (string): No regular columns identified under this header.
- `ProjectGroup` (string): No regular columns identified under this header.
- `ProjectGroupDescription` (string): No regular columns identified under this header.
- `ProjectKey` (int64): No regular columns identified under this header.
- `ProjectleaderId` (string): No regular columns identified under this header.
- `SalesPriceBaseCurrency` (decimal): No regular columns identified under this header.
- `Startdate` (dateTime): No regular columns identified under this header.
- `Status` (string): No regular columns identified under this header.
- `Statuscode` (string): No regular columns identified under this header.
- `TotalContractSum` (decimal): No regular columns identified under this header.

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
_(No measures identified in this table file.)_

---

#### Table: `vw_missing_timesheet`

The `vw_missing_timesheet` table appears to be designed to track and analyze missing timesheet entries for employees, including details such as hours worked, contract hours, and various flags indicating the status of timesheets. The presence of measures related to missing hours and employee counts suggests a focus on identifying discrepancies in timesheet reporting.

##### Regular Columns
- `ActualHours` (double): No regular columns identified under this header.
- `Approved` (boolean): No regular columns identified under this header.
- `ContractEndDate` (dateTime): No regular columns identified under this header.
- `ContractHours` (int64): No regular columns identified under this header.
- `ContractStartDate` (dateTime): No regular columns identified under this header.
- `ContractStatusToday` (string): No regular columns identified under this header.
- `EmployeeID` (string): No regular columns identified under this header.
- `EmployeeName` (string): No regular columns identified under this header.
- `Hours_` (double): No regular columns identified under this header.
- `IngestDatetime` (dateTime): No regular columns identified under this header.
- `ManagerID` (string): No regular columns identified under this header.
- `ManagerName` (string): No regular columns identified under this header.
- `MissingHours` (double): No regular columns identified under this header.
- `Project` (string): No regular columns identified under this header.
- `ProjectCode` (string): No regular columns identified under this header.
- `Rejected` (boolean): No regular columns identified under this header.
- `ReportReady` (boolean): No regular columns identified under this header.
- `SalesAmount` (double): No regular columns identified under this header.
- `SalesPrice` (double): No regular columns identified under this header.
- `ShortDate` (dateTime): No regular columns identified under this header.
- `SubUnit` (string): No regular columns identified under this header.
- `TimesheetCode` (string): No regular columns identified under this header.
- `TimesheetDescription` (string): No regular columns identified under this header.
- `Week_` (int64): No regular columns identified under this header.
- `Year_` (int64): No regular columns identified under this header.

##### Calculated Columns
- **`BillableDep`**:
  ```dax
IF(
    ISBLANK(RELATED(lkp_Unit[BillableDep])), 
    1, 
    IF(RELATED(lkp_Unit[BillableDep]) <> 0, 1, 0)
)
  ```

- **`CC_ActiveEmployees`**:
  ```dax
IF(vw_missing_timesheet[ShortDate] >= vw_missing_timesheet[ContractStartDate] && 
  (vw_missing_timesheet[ShortDate] <= vw_missing_timesheet[ContractEndDate] || ISBLANK(vw_missing_timesheet[ContractEndDate]))
    , 1
    , 0
)
  ```

- **`IncompleteFlag`**:
  ```dax
if(vw_missing_timesheet[MissingHours] < 0, 1, 0)
  ```

- **`MAIN_UNIT`**:
  ```dax
IF(
    ISBLANK(RELATED(lkp_Unit[Unit])), 
    "Unknown", 
    RELATED(lkp_Unit[Unit])
)
  ```

- **`YearWeek`**:
  ```dax
YEAR([ContractEndDate]) & "-" & 
FORMAT(WEEKNUM([ContractEndDate]), "00")
  ```

##### Measures (defined in this table file)
- **`CountNegativeMissingHours`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT('vw_missing_timesheet'[EmployeeID]),
    'vw_missing_timesheet'[MissingHours] < 0
)
  ```

- **`Count_Negative_Consultant`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT('vw_missing_timesheet'[EmployeeName]),
    'vw_missing_timesheet'[MissingHours] < 0
)
  ```

- **`Dax_EmpCount`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT(vw_missing_timesheet[EmployeeName])
)
  ```

- **`Dax_EmpCount_MissingTS`**:
  ```dax
VAR ActiveEmployees = 
    FILTER(
        SUMMARIZE(
            vw_missing_timesheet,
            vw_missing_timesheet[EmployeeName],
            vw_missing_timesheet[Year_],
            vw_missing_timesheet[Week_],
            lkp_Unit[Unit],
            vw_missing_timesheet[SubUnit],
            "MissingHours", SUM(vw_missing_timesheet[Hours_]) - MAX(vw_missing_timesheet[ContractHours]),
            "MinActiveEmp", MIN(vw_missing_timesheet[CC_ActiveEmployees])
        ),
        [MinActiveEmp] = 1 && [MissingHours] < 0
    )
RETURN COUNTROWS(ActiveEmployees)
  ```

- **`Dax_MissingHours`**:
  ```dax
VAR ActiveEmployees = 
    FILTER(
        SUMMARIZE(
            vw_missing_timesheet,
            vw_missing_timesheet[EmployeeName],
            vw_missing_timesheet[Year_],
            vw_missing_timesheet[Week_],
            lkp_Unit[Unit],
            vw_missing_timesheet[SubUnit],
            "MissingHours", SUM(vw_missing_timesheet[Hours_]) - MAX(vw_missing_timesheet[ContractHours]),
            "MinActiveEmp", MIN(vw_missing_timesheet[CC_ActiveEmployees])
        ),
        [MinActiveEmp] = 1 && [MissingHours] < 0
    )
RETURN 
    SUMX(ActiveEmployees, [MissingHours])
  ```

- **`ExpectedContractHoursWeekly`**:
  ```dax
SUMX(
    SUMMARIZE(
        vw_missing_timesheet,
        vw_missing_timesheet[Week_],
        vw_missing_timesheet[EmployeeID],
        "ContractHours", MAX(vw_missing_timesheet[ContractHours])
    ),
    [ContractHours]
)
  ```

- **`MISSING%`**:
  ```dax
DIVIDE([Dax_EmpCount]-[Dax_EmpCount_MissingTS],[Dax_EmpCount]) * 100
  ```

- **`MissingHoursOut`**:
  ```dax
MAX(vw_missing_timesheet[ContractHours]) - MAX(vw_missing_timesheet[Hours_])
  ```

- **`PercentageCompleteness`**:
  ```dax
DIVIDE(
    SUM(vw_missing_timesheet[MissingHours])- [ExpectedContractHoursWeekly] ,
    [ExpectedContractHoursWeekly],
    0
)
  ```

- **`UniqueEmployeesPerUnit`**:
  ```dax
SUMX(
    SUMMARIZE(
        vw_missing_timesheet,
        vw_missing_timesheet[Week_],
        vw_missing_timesheet[EmployeeName],
        vw_missing_timesheet[MAIN_UNIT],
        "IncompleteFlag", MAX(vw_missing_timesheet[IncompleteFlag])
    ),
    [IncompleteFlag]
)
  ```

---

#### Table: `vw_Timesheet`

The `vw_Timesheet` table appears to be designed for tracking timesheet data, including hours worked, project details, employee information, and contract statuses. It includes various measures for calculating hours, statuses, and financial metrics, as well as columns for employee and project identification.

##### Regular Columns
- `Approved` (boolean): No regular columns identified under this header.
- `BillableDep` (Unknown)
- `BillablePrj` (Unknown)
- `Client` (string): No regular columns identified under this header.
- `Code2` (string): No regular columns identified under this header.
- `ContractEndDate` (dateTime): No regular columns identified under this header.
- `ContractStartDate` (dateTime): No regular columns identified under this header.
- `ContractTransferDate` (dateTime): No regular columns identified under this header.
- `CostAmount` (double): No regular columns identified under this header.
- `CostPrice` (double): No regular columns identified under this header.
- `Currency` (string): No regular columns identified under this header.
- `DebtorName` (string): No regular columns identified under this header.
- `EmployeeID` (string): No regular columns identified under this header.
- `EmployeeKey` (int64): No regular columns identified under this header.
- `EmployeeName` (string): No regular columns identified under this header.
- `Employer` (string): No regular columns identified under this header.
- `EmployerCode` (int64): No regular columns identified under this header.
- `ExtraDetails` (string): No regular columns identified under this header.
- `FinancialYear` (int64): No regular columns identified under this header.
- `GroupCat` (Unknown)
- `Hours` (double): No regular columns identified under this header.
- `HoursType` (string): No regular columns identified under this header.
- `HoursTypeCode` (string): No regular columns identified under this header.
- `HoursperWeek` (int64): No regular columns identified under this header.
- `IngestDatetime` (dateTime): No regular columns identified under this header.
- `InternalPMID` (string): No regular columns identified under this header.
- `InternalPMKey` (int64): No regular columns identified under this header.
- `IsContractActive` (Unknown)
- `JobTitle` (string): No regular columns identified under this header.
- `MAIN_UNIT` (Unknown)
- `ManagerID` (string): No regular columns identified under this header.
- `ManagerKey` (int64): No regular columns identified under this header.
- `MonthNumber` (Unknown)
- `OHWWorkBookings` (string): No regular columns identified under this header.
- `Project` (string): No regular columns identified under this header.
- `ProjectCode` (string): No regular columns identified under this header.
- `ProjectProfile` (string): No regular columns identified under this header.
- `ProjectProfileCode` (int64): No regular columns identified under this header.
- `ProjectType` (string): No regular columns identified under this header.
- `QualifyPrj` (Unknown)
- `Rejected` (boolean): No regular columns identified under this header.
- `ReportReady` (boolean): No regular columns identified under this header.
- `ReportingEntity` (Unknown)
- `SalesAmount` (double): No regular columns identified under this header.
- `SalesPrice` (double): No regular columns identified under this header.
- `Status` (string): No regular columns identified under this header.
- `Taxed` (boolean): No regular columns identified under this header.
- `Tier` (string): No regular columns identified under this header.
- `TimesheetCode` (string): No regular columns identified under this header.
- `TimesheetDate` (dateTime): No regular columns identified under this header.
- `TimesheetDescription` (string): No regular columns identified under this header.
- `Unit` (string): No regular columns identified under this header.
- `UnitCode` (string): No regular columns identified under this header.
- `WorkEmail` (string): No regular columns identified under this header.
- `cc_Employer` (Unknown)

##### Calculated Columns
_(No calculated columns identified.)_

##### Measures (defined in this table file)
- **`'MSR_Cumulative Cost'`**:
  ```dax
VAR SelectedMonth = MAX('DimDate'[MonthNumberOfYear])  -- Get the selected month in the visual
RETURN 
CALCULATE(
    SUM(vw_Timesheet[CostAmount]),
    FILTER(
        ALLSELECTED('DimDate'),  
        'DimDate'[MonthNumberOfYear] <= SelectedMonth  -- Include all months up to the current one
    )
)
  ```

- **`'MSR_Cumulative Sales'`**:
  ```dax
VAR SelectedMonth = MAX('DimDate'[MonthNumberOfYear])  -- Get the selected month in the visual
RETURN 
CALCULATE(
    SUM(vw_Timesheet[SalesAmount]),
    FILTER(
        ALLSELECTED('DimDate'),  
        'DimDate'[MonthNumberOfYear] <= SelectedMonth  -- Include all months up to the current one
    )
)
  ```

- **`'Static Total Employees'`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT(vw_Timesheet[EmployeeName]), 
    ALL(vw_Timesheet),
    vw_Timesheet[ContractStatusToday] = "Valid"
)
  ```

- **`ContractStatus2`**:
  ```dax
IF(
    [ContractStatusMeasure] = "Active",
    [HoursDifference],
    "Not Active"
)
  ```

- **`ContractStatusMeasure`**:
  ```dax
IF(
    ISBLANK(MAX('vw_Timesheet'[ContractEndDate])) || MAX('vw_Timesheet'[ContractEndDate]) > TODAY(),
    "Active",
    "Not Active"
)
  ```

- **`CurrentWeekCard`**:
  ```dax
WEEKNUM(TODAY())
  ```

- **`DISTINCT_COUNT_EMP`**:
  ```dax
DISTINCTCOUNT(vw_Timesheet[EmployeeName])
  ```

- **`Dax_EmpCount_Approved`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT(vw_Timesheet[EmployeeName]),
    vw_Timesheet[Approved] = FALSE()
)
  ```

- **`Dax_EmpCount_RReady`**:
  ```dax
CALCULATE(
    DISTINCTCOUNT(vw_Timesheet[EmployeeName]),
    vw_Timesheet[ReportReady] = FALSE()
)
  ```

- **`HoursDifference`**:
  ```dax
SUM('vw_Timesheet'[Hours]) - MAX('vw_Timesheet'[HoursperWeek])
  ```

- **`MSR_AVG_HOURLY_RATE`**:
  ```dax
SUM(vw_Timesheet[SalesAmount])/SUM(vw_Timesheet[Hours])
  ```

- **`MSR_Cumulative_Revenue`**:
  ```dax
CALCULATE(
    SUM(vw_Timesheet[SalesAmount]),
    FILTER(
        ALLSELECTED(DimDate[ShortDate]),
        DimDate[ShortDate] <= MAX(DimDate[ShortDate])
    )
)
  ```

- **`MSR_PROJECT_%_REALIZATION`**:
  ```dax
IF(sum(tbl_Project[SalesPriceBaseCurrency]) <> 0, sum(vw_Timesheet[SalesAmount])/sum(tbl_Project[SalesPriceBaseCurrency]))
  ```

- **`MSR_ProjectMargin`**:
  ```dax
sum(vw_Timesheet[SalesAmount]) - sum(vw_Timesheet[CostAmount])
  ```

- **`Msr_ActiveContacts`**:
  ```dax
VAR _MaxDate =
    MAX ( DimDate[ShortDate])
VAR _MinDate =
    MIN ( DimDate[ShortDate] )
VAR _StartsBeforeMonthEnd =
    CALCULATETABLE ( VALUES ( vw_Timesheet[EmployeeID] ),DimDate[ShortDate] <= _MaxDate )
VAR _notEndAfterMonthEnd =
    CALCULATETABLE (
        VALUES ( vw_Timesheet[EmployeeID] ),
        DimDate[ShortDate] >= _MinDate
            || ISBLANK ( DimDate[ShortDate] ),
        USERELATIONSHIP ( DimDate[ShortDate], vw_Timesheet[ContractEndDate] )
    )
RETURN
    COUNTROWS ( INTERSECT ( _notEndAfterMonthEnd, _StartsBeforeMonthEnd ) )
  ```

- **`Msr_HoursPercentage`**:
  ```dax
VAR TotalValue = 
    CALCULATE(SUM(vw_Timesheet[Hours]), 
        ALLSELECTED(vw_Timesheet[GroupCat], vw_Timesheet[Project], vw_Timesheet[EmployeeName])
        )
RETURN 
    DIVIDE(SUM(vw_Timesheet[Hours]), TotalValue, 0)
  ```

- **`TotalActualHours`**:
  ```dax
SUM(vw_Timesheet[Hours])
  ```

- **`TotalContractedHours`**:
  ```dax
SUMX(
    VALUES(vw_Timesheet[EmployeeName]),
    MAX(vw_Timesheet[HoursPerWeek])
)
  ```

- **`TotalHoursDeltaComplete`**:
  ```dax
DIVIDE(
    [TotalActualHours],
    [TotalContractedHours],
    0
)
  ```

- **`TotalHoursDeltaCompleteNr`**:
  ```dax
[TotalActualHours]-[TotalContractedHours]
  ```

- **`TotalHoursTracked`**:
  ```dax
IF (
    ISBLANK(SUM(vw_Timesheet[Hours])),
    0,
    SUM(vw_Timesheet[Hours])
)
  ```

- **`TotalHoursTrackedMissing`**:
  ```dax
IF (
    ISBLANK(SUM(vw_Timesheet[Hours])),
    "Empty",
    SUM(vw_Timesheet[Hours])
)
  ```

- **`UTILIZATION_New`**:
  ```dax
-- Calculate the Billable Hour Ratio for Active Employees
VAR _TotalBillableHours = [UTI_TotalBillableHours]
VAR _TotalHours = [UTI_TOTALHOURS]

RETURN
    IF(NOT(ISBLANK([Msr_ActiveContacts])), 
            IF(NOT(ISBLANK([UTI_TOTALHOURS])), 
                IF(ISBLANK(DIVIDE(_TotalBillableHours, _TotalHours)),0,DIVIDE(_TotalBillableHours, _TotalHours))
            )
    )
  ```

- **`UTI_TOTALHOURS`**:
  ```dax
CALCULATE(SUM(vw_Timesheet[Hours]), vw_Timesheet[QualifyPrj] = 1)
  ```

- **`UTI_TotalBillableHours`**:
  ```dax
Var FilteredHours =
    Filter(
        vw_Timesheet,
        (
        vw_Timesheet[QualifyPrj] = 1 && 
        vw_Timesheet[BillablePrj] = 1) ||
            SEARCH("Customer Success Services",vw_Timesheet[Project],1,0) > 0
    )
RETURN 
    SUMX(FilteredHours, vw_Timesheet[Hours])
  ```

- **`trackedDiff`**:
  ```dax
[TotalHoursTracked] - 
    CALCULATE(
        MAXX(DISTINCT(vw_Timesheet[HoursPerWeek]), vw_Timesheet[HoursPerWeek]),
        ALLEXCEPT(vw_Timesheet, vw_Timesheet[EmployeeID])
    )
  ```

- **`trackedDiff2`**:
  ```dax
[TotalHoursTracked] - 
    CALCULATE(
        MAXX(
            DISTINCT(vw_Timesheet[HoursPerWeek]), 
            vw_Timesheet[HoursPerWeek]
        ),
        REMOVEFILTERS(vw_Timesheet[HoursPerWeek]),
        ALLEXCEPT(vw_Timesheet, vw_Timesheet[EmployeeID])
    )
  ```

---

### <a name="22-relationships"></a>2.2 Relationships

The following relationships were identified based on explicit TMDL definitions:

* `vw_Timesheet`.[`ProjectCode`] **->** `tbl_Project`.[`Project`] _(Inactive, Filter: bothDirections)_

---

## <a name="3-visuals-based-on-configuration"></a>3. Visuals (Based on Configuration)

_(No visuals discovered or documented. Visual processing might be disabled or failed.)_

